"""
CoinMarketCap adapter stub.

The original adapter was removed intentionally. This module now raises ImportError
when imported so that accidental imports fail loudly. If you need a full adapter
again, reimplement it here or restore from version control.
"""

raise ImportError("CoinMarketCap adapter removed from the codebase; re-add if needed")